[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "yule-security"
version = "0.1.0"
description = "보안 헬퍼 (paste guard 등). 출력/입력 secret 차단."
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
