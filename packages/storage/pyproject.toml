[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "yule-storage"
version = "0.1.0"
description = "Local-first SQLite stores for Yule Studio agents: JSON cache, calendar-state sync, task history (stdlib + sqlite only)."
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
