[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "yule-learning"
version = "0.1.0"
description = "학습 ledger — mistake_ledger / preflight (운영 메모리)."
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
