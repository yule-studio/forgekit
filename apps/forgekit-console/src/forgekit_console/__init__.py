"""Forgekit operator console — a Textual TUI over the yule runtime/harness surfaces.

The pure core (``models`` / ``commands`` / ``data``) is stdlib-only and importable
without textual; the ``tui`` layer + ``app.main`` entrypoint add the terminal UI.
"""

__version__ = "0.1.0"
